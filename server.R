library(plotly)
library(quantmod)
library(TTR)
library(tseries)
library(forecast)
library(glue)
library(dplyr)

library(shiny)

shinyServer(function(input, output) {
  
  plot_ticker <- function(ticker){
    
    
  }

  output$distPlot <- renderPlot({
    
  })

})
